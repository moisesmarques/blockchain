package main

const (
	apKey              = iota
	computeKeyGpu      = iota
	appKey             = iota
	orchestrationKey   = iota
	computeKeyCpu      = iota
	deployerAccountKey = iota
	networkKey         = iota
	connectionKey      = iota
	computeKeyData     = iota
)
